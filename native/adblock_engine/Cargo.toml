[package]
name = "adblock_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
adblock = { version = "0.9", default-features = false, features = ["full-regex-handling"] }
rmp = "0.8.10"
rmp-serde = "1.1"

[profile.release]
panic = "abort"
lto = true
