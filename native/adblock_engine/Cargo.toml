[package]
name = "adblock_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
adblock = { version = "0.13", default-features = false, features = ["full-regex-handling"] }

[profile.release]
panic = "abort"
lto = true
